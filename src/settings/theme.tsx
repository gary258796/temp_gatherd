import { createTheme } from "@mui/material";

const mainGreen = '#2b403a'

export const theme = createTheme({
  palette: {
    primary: {
      main: mainGreen
    }
  },
  typography: {
    fontFamily: 'NotoSansTC-Regular'
  }
});
